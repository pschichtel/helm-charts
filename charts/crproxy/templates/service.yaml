apiVersion: v1
kind: Service
metadata:
  name: {{ include "crproxy.fullname" . }}
  labels:
    {{- include "crproxy.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crproxy.selectorLabels" . | nindent 4 }}
