apiVersion: v1
kind: Service
metadata:
  name: docspell-restserver
  labels:
    app: docspell-restserver
spec:
  type: ClusterIP
  ports:
    - port: 7880
      targetPort: 7880
      protocol: TCP
      name: api
  selector:
    app: docspell-restserver