{{- range $repo := .Values.repos }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ print "restic-forget-" $repo.name "-secrets" | quote }}
data:
    RESTIC_REPOSITORY: {{ $repo.repo | quote }}
    RESTIC_PASSWORD: {{ $repo.password | quote }}
    {{- range $name, $value := $repo.extraEnv }}
    {{ $name }}: {{ $value | quote }}
    {{- end }}
{{- end }}