apiVersion: v1
kind: Service
metadata:
  name: {{ include "photon.fullname" . }}
  labels:
    {{- include "photon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "photon.selectorLabels" . | nindent 4 }}
