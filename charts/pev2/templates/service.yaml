apiVersion: v1
kind: Service
metadata:
  name: {{ include "pev2.fullname" . }}
  labels:
    {{- include "pev2.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pev2.selectorLabels" . | nindent 4 }}
